# ADR-016「Halo」UI全面刷新 + 自動テスト導入 — 適用ノート

このファイルは `DECISION_LOG.md` に追記するADR本文と、適用手順・テスト結果を
1つにまとめたものです。既存のADR-001〜015のフォーマットに合わせています。

---

> **[ADR-016] UI全面刷新「Halo」— 円(orbit)を統一モチーフにした操作体系へ、
> カメラ切替の廃止、影調整の1ジェスチャー化、自動テストスイートの導入**
>
> - **ステータス:** 承認済(hinya本人の直接指示によるスコープ変更。ADR-015「The
>   Void」が「操作がわかりにくい」という評価だったため、同方式を撤回し全面刷新)
> - **コンテキスト:** 「OshiCamera/devの両方でUIが著しく悪い」「ボタンが分かり
>   にくい」「影の手動設定を簡単に」「キャラクター選択画面をもっと面白く」「カメラ
>   切替を廃止」「ホーム画面アイコンを直す(Altair_SPA.htmlのアイコン手法を参照)」
>   「アニメーションを積極導入」という直接指示があった。あわせて、これまでADR-013
>   /014/015で「dev.htmlはスコープ外」としてきた方針を撤回し、dev.htmlも見た目の
>   刷新対象に含めることが明示された。
> - **検討した代替案:**
>   1. v2「The Void」の微調整継続 → 「今ので全然だめ」という評価のため不採用。
>   2. 影調整をスライダーのまま個数だけ減らす → 「もっと簡単に」の要求を満たさない
>      (操作回数は減らせても、向きと長さを別々に触る構造自体は変わらないため)。
>   3. dev.jsのロジック自体も書き直す → 較正(calibration-tool.js)・環境シミュ
>      レータ(dev-environment.js)等、実機/dev.htmlでの動作実績があるロジックを
>      道連れにするリスクが大きく、「見た目は良くなったが機能が壊れた」という
>      最悪の結果を避けるため、今回はCSS/ラベル/アニメーションの化粧直しに限定した
>      (3参照)。
> - **デザイン方針(合言葉「Halo」):** 円(軌道の光)を全体の統一モチーフにする。
>   シャッター・キャラクターバッジ・影調整パッド・ローディングスピナー・ドックの
>   展開のすべてに同じ「軌道の光」のビジュアル言語を使うことで、「ミニマルにする
>   ほど機能がわからなくなる」というThe Voidの反省を踏まえ、要素を隠しすぎず、
>   ラベル・輪郭・動きで「何を押すと何が起きるか」を常に示す方針にした。配色は
>   Flowly/Altairと同じ「黒地+アクセント色」のファミリーを踏襲し、アクセントは
>   引き続き金(`--gold:#e7b94c`)を基調とした(選択画面のみキャラクター別の
>   アクセント色を光彩に使う)。
> - **実装内容:**
>   1. **カメラ切替の廃止(hinya指示)。** `switch-cam-btn`・`facingMode`・
>      `FOV_BY_FACING`・ミラー表示ロジックをmain.js/index.htmlから完全に削除し、
>      常に背面カメラ(environment)のみを使う構成へ単純化した(AR配置・投影補正の
>      前提を単純化する副次効果もある)。
>   2. **「ファン・ドック」への刷新。** 右上「⋯」の縦一列ドックを廃止し、トリガー
>      から円弧状(1/4円)に5個のボタンが展開する方式に変更。各ボタンにアイコン+
>      文言ラベルを併記し(「位置」「影」「タイマー」「写真/動画」「リセット」)、
>      「ボタンが分かりにくい」の指摘に対応した。アングル切替(全身/上半身/顔アップ)
>      は逆に「隠さない」方針に変更し、常時表示の細帯として残した(頻繁に使う操作は
>      隠さない、という原則をドックと使い分けた)。
>   3. **影の手動調整パネルを「ハロー・ダイヤル」方式へ全面書き換え
>      (`js/shadow-controls-ui.js`)。** 旧: スライダー2本(向き/長さ)+トグルの
>      計3操作。新: 円形パッド内で太陽の印をドラッグするだけで、向き(角度)と
>      長さ(中心からの距離)を1ジェスチャーで同時に決められる。中心=影が短い
>      (太陽が高い)、外周=影が長い(太陽が低い)という直感的な対応にした。
>      角度は上(12時)=0°、時計回りに-180〜180°。既存の`setShadowDirection`/
>      `setShadowLength`/`resetShadowManualOverride`のAPIは一切変更していない
>      (shadow-rig.js/directional-shadow.js側は無改修)。
>   4. **キャラクター選択画面の全面刷新。** 横スクロール式(scroll-snap)の
>      カルーセルに変更し、キャラクターごとに「軌道バッジ(呼吸するリング+回転する
>      光彩)」「名前」「キャッチコピー」「専用の光彩色」を表示する。この光彩色/
>      コピーは`js/characters-data.js`に追加した`themeColor`/`tagline`という
>      UI表示専用の追加メタデータで、ポーズ/表情/ボーン定義には一切影響しない
>      (未知のキーは無視されるため安全な追加。既存のポーズ数値は一切変更していない
>      — 本ADR末尾の「テスト結果」で数値の無改変を機械的に検証済み)。
>   5. **ホーム画面アイコンの刷新。** Flowlyの前身であるAltair(`Altair_SPA.html`)
>      のアイコン生成手法(canvas上に8方向スターをグラデーション線で描き、角丸の
>      黒バッジに載せる。画像ファイルを追加しないノービルド方式)を技術的に踏襲し、
>      配色のみOshiCameraのブランド色(黒`#07070c`×金)に置き換えた。中央に小さな
>      シャッター円のモチーフを追加し、カメラアプリであることを示している。
>   6. **アニメーションの積極導入。** 星屑の瞬き・背景ブロブのドリフト・軌道の
>      回転(conic-gradient)・カプセルのスプリング切替・ドックのファン展開・
>      撮影結果画面の登場演出・シャッター発火時の二重波紋(nova ring)等を追加。
>      `prefers-reduced-motion`を尊重し、設定されている場合は全アニメーションを
>      実質無効化する。
>   7. **dev.html/dev.js/dev-scene-panel.css の視覚統一(スコープ復帰)。**
>      CONSTRAINTS.md 1節が定めていた「dev.htmlはUI刷新の対象外」を撤回し、
>      配色トークン(`--gold`/`--glass`/`--line`等)と控えめなフェードインを
>      本番側と揃えた。**dev.js自体(OrbitControls・較正・環境シミュレータ等の
>      ロジック)は1行も変更していない**(上記「検討した代替案」3参照)。
>   8. **自動テストスイートの新規導入(下記参照)。** これまでのADRには
>      「実機確認待ち」のみで機械的な検証手段が無かったが、今回は
>      Node.js+jsdomによるDOM/ロジックの自動テストを整備し、実機に出す前の
>      レイヤーとして機械的に検証できる範囲を広げた。
> - **開発上の教訓・自動テストで実際に見つかったバグ:**
>   ハロー・ダイヤル(3番)のドラッグ計算に、`Math.atan2(0, -0)`が180°を返す
>   というIEEE754の仕様上の癖に起因する不具合があった。パッドの中心をタップ
>   しただけなのに、影の向きが真後ろ(180°)へ勝手に飛んでしまう、というもの。
>   自動テスト(下記)で角度計算を全周サンプリングした際に検出し、「長さが
>   ほぼ0(中心付近)の間は向きの値を更新せず、直前の向きを保持する」という
>   デッドゾーンを追加して修正した。**実機で人間が目視するまで気づけなかった
>   可能性が高い種類のバグであり、自動テスト導入の効果を示す実例。**
> - **既知の限界・実機確認が必須な項目(CONSTRAINTS.md 6節の運用方針通り):**
>   Claudeはブラウザ実機でのレンダリング結果・タップの触感を直接確認できない
>   ため、以下は「実装はしたが目視・触感の確認なし」の状態である。
>   - ファン・ドックの展開アニメーションの速さ・指の届きやすさ(5個のボタンが
>     画面右上から扇状に広がる際、小さい画面幅で他要素と干渉しないか)
>   - ハロー・ダイヤルの実際の操作感(パッドの半径78pxが指の太さに対して
>     適切か、ドラッグ中の追従の滑らかさ)
>   - キャラクター選択カルーセルのscroll-snapの挙動(iOS Safariでの慣性
>     スクロールとの相性、ページング感)
>   - 新アイコンが実際にホーム画面へ正しいサイズ・角丸で表示されるか
>   - 新アニメーション群(星屑・軌道回転等)がiPhone 14で滑らかに動くか
>     (GPU負荷の実測なし)
> - **影響・結果:** `switch-cam-btn`関連のコードパスが完全に削除された以外は、
>   AR配置・投影補正・環境認識・ShadowRig本体・MMDキャラクター制御(`character.js`
>   /`character/`以下)には一切触れていない。問題が起きた場合は本パッチが変更した
>   ファイルだけを差し戻せば旧UI(v2「The Void」、カメラ切替あり)に戻せる。

---

## 適用対象ファイル

以下のみを変更・新規追加した。**一覧に無いファイル(`character.js`、
`js/shadow/*`、`js/environment*`、`js/lighting.js`、`js/postfx.js`、
`js/diagnostics.js`、`js/environment-analyzer.js`等)は無改造。**

| ファイル | 種別 | 変更内容 |
|---|---|---|
| `index.html` | 変更 | 選択画面カルーセル化、ファン・ドック化、影パネルのダイヤル化、新アイコン生成スクリプト、`switch-cam-btn`削除 |
| `style.css` | 全面書き換え | 「Halo」トークン・アニメーション一式 |
| `main.js` | 変更 | カメラ切替機能の完全削除、選択画面カード生成ロジックの刷新、mode/timerボタンのラベル更新ロジック追加。**AR/MMD/影/環境認識ロジックは無変更** |
| `js/shadow-controls-ui.js` | 全面書き換え | ハロー・ダイヤル方式。公開APIの呼び出し先(`shadowRig`)は無変更 |
| `js/ui-gestures.js` | 全面書き換え | ファン・ドック開閉、nova ring追加。既存の結果画面スワイプは踏襲 |
| `js/pose-ring.js` | 変更 | 公開API(`window.PoseRing.init/setActive/syncPosition`)は完全維持。ドット・インジケータ(`#dock-dots`)を追加しただけ |
| `js/characters-data.js` | 追記のみ | 各キャラクターに`themeColor`/`tagline`を追加。**ポーズ/表情/ボーン数値は1つも変更していない**(下記テスト参照) |
| `dev.html` | 変更 | 構造・ID・`dsp-tab-calib`等は無改造。コメントのみ追記 |
| `dev.css` / `dev-scene-panel.css` | 変更 | 配色トークンをHaloへ統一、フェードイン追加。DOM ID/構造は無改造 |
| `dev.js` | **無改造** | 同梱はしているが中身は1バイトも変更していない(ドロップイン時の参照用) |

---

## テスト結果(今回新規導入した自動テスト、Node.js + jsdom)

CONSTRAINTS.md 6節の通り、Claudeはブラウザ実機のレンダリング・タップ感触を
直接確認できないため、「実機に出す前に機械的に検証できる範囲」を今回できる
限り広げた。すべてこのサンドボックス内でNode.js(jsdom/cssパーサー)を使って
実行し、**全項目パスを確認済み**。

1. **構文チェック**: 変更した全JSファイルに対し`node --check`
   (`.mjs`一時コピー経由、ADR-014の教訓に従う) → 全ファイルOK。
2. **重複宣言チェック**: トップレベルの`const`/`let`重複が無いか機械的に確認
   (ADR-011の教訓) → 重複なし。
3. **DOM ID突合監査**: `main.js`/`js/ui-gestures.js`/`js/pose-ring.js`/
   `js/shadow-controls-ui.js`が参照する全`getElementById(...)`が
   `index.html`に実在するか、`dev.js`が参照する全IDが`dev.html`に実在するかを
   スクリプトで突合 → 不足なし。
4. **CSS構文検証**: `style.css`/`dev.css`/`dev-scene-panel.css`を実際の
   CSSパーサー(`css`パッケージ)で解析 → 3ファイルとも構文エラーなし
   (それぞれ216・28・39ルール)。
5. **ポーズ/ボーン数値の無改変検証(最重要)**: `js/characters-data.js`の
   全ポーズについて、ボーン数・数値の合計・`wiggle`/`centerOffset`・
   `expressions`・`idle`設定を、変更前の原本と1件ずつ突合。
   **→ 天音かなた・音乃瀬奏とも全ポーズが原本と完全一致(追加したのは
   `themeColor`/`tagline`の2フィールドのみ)。**
6. **キャラクター選択カルーセルのロジックテスト**: `characters-data.js`から
   カード(スライド)・ドットを生成する処理を実データで実行し、枚数・
   `themeColor`反映・タグライン表示・タップ選択→`currentCharacterIndex`更新
   →選択画面クローズの一連の流れを検証 → 12/12項目パス。
7. **ファン・ドック/ポーズカプセルの操作テスト(jsdom)**: 実際の
   `js/pose-ring.js`・`js/ui-gestures.js`を仮想DOM上で読み込み、
   カテゴリ切替・「次へ」タップ・ドット追従・ドックの開閉・ファンボタン
   タップ後の自動クローズ(約260ms後)を検証 → 15/15項目パス。
8. **影のハロー・ダイヤルのロジックテスト(jsdom、実モジュールを直接使用)**:
   `js/shadow-controls-ui.js`を実際にインポートし、パネルの開閉・手動トグル
   ON/OFF時のAPI呼び出し(`setShadowDirection`/`setShadowLength`/
   `resetShadowManualOverride`)・リセットボタンの挙動を検証 → 13/13項目パス。
9. **ドラッグ角度/長さ計算の数理検証**: 上下左右・45度・パッド外へのドラッグ・
   全周360度(15度刻み)×5段階の長さで、角度⇄座標の往復一致性、および
   0〜100%/−180〜180°の範囲逸脱・`NaN`が発生しないことを検証。
   **この過程で「パッド中心をタップすると向きが180°へ勝手に飛ぶ」という
   実バグを発見し、修正・再検証した(上記「開発上の教訓」参照)。**
10. **ホーム画面アイコン生成スクリプトの検証**: 実際のcanvas APIをモック化し、
    アイコン生成コードを実行。描画コマンドに`NaN`/`Infinity`が渡っていないか、
    期待通り8サイズ分の`apple-touch-icon`リンクが生成されるかを確認 →
    正常終了・不正値なし・8サイズ生成を確認。
11. **HTML構造検証**: `index.html`/`dev.html`をjsdomでパースし、
    `#switch-cam-btn`が存在しないこと、ファン・ドックのボタンが5個
    存在すること等を確認。

### 自動テストで検証できていない項目(上記ADR本文の「既知の限界」と同一)

実際の指のジェスチャー感触・アニメーションの滑らかさ・GPU負荷・
scroll-snapの慣性挙動など、**「動いて見える/触った感じが良いか」は
このサンドボックスでは検証不可能**であり、これらは引き続き実機
(iPhone 14 Safari)での確認をお願いしたい。

---

## ROADMAP.md への追記案

- [ ] 【最優先・新規】ADR-016「Halo」の実機確認
  - [ ] ファン・ドックの展開が指の届く範囲か、他要素と干渉しないか
  - [ ] ハロー・ダイヤルの操作感(パッド半径・追従の滑らかさ)
  - [ ] キャラクター選択カルーセルのスクロール感(iOS Safari)
  - [ ] 新ホーム画面アイコンの表示確認
  - [ ] 新アニメーション群のパフォーマンス(iPhone 14での滑らかさ)
- [x] dev.html/dev.css/dev-scene-panel.cssの見た目統一(ADR-016で対応、
      CONSTRAINTS.md 1節の「dev.htmlはスコープ外」は撤回)
